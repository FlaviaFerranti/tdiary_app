<% provide(:title, "Pagina iniziale") %>

<% if user_signed_in? %>
    <div class="row">
        <aside class="col-md-4">
            <section class="user_info">
                <%= render 'shared/user_info' %>
            </section>
            <section class="travel_form">
                <%= render 'shared/stats' %>
            </section>
            <section class="travel_form">
                <%= render 'shared/travel_form' %>
            </section>
        </aside>
        <div class="col-md-8">
            <h3>Travel Feed</h3>
            <%= render 'shared/feed' %>
        </div>
    </div>
<% else %>

    <div class="center jumbotron">
    <h1>Benvenuto su Travel Diary</h1>

    <h2>Grazie alla nostra applicazione avrai la possibilità di condividere con altri utenti esperienze vissute e viaggi indimenticabili.</h2>

    <p>
    Comincia subito!
    </p>
    <%= link_to "Registrati ora!", new_user_registration_path, class: "btn btn-lg btn-primary", id:'bottone' %>
    </div>

<% end %>


